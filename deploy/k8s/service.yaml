apiVersion: v1
kind: Service
metadata:
  name: compliance-copilot
  labels:
    app: compliance-copilot
spec:
  type: ClusterIP
  selector:
    app: compliance-copilot
  ports:
    - name: http
      port: 80
      targetPort: http
