# Core runtime dependencies, pinned for reproducible builds.
# This project runs fully offline by default: FAISS (local vector store) +
# MockLLM (deterministic, no network). Real provider SDKs below are wired
# in behind an interface (see src/compliance_copilot/infrastructure/llm/)
# and are only exercised if a corresponding API key env var is set.

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

langchain==0.2.6
langchain-community==0.2.6
langchain-core==0.2.10
langchain-text-splitters==0.2.2

faiss-cpu==1.8.0.post1
rank-bm25==0.2.2
numpy==1.26.4

tenacity==8.4.2
python-json-logger==2.0.7
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0

python-dotenv==1.0.1
