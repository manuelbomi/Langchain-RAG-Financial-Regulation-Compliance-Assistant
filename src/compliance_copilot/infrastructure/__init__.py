"""Infrastructure layer: concrete adapters (vector store, LLM providers,
config, logging, tracing, security). Implements interfaces consumed by the
service layer; contains all I/O and third-party library coupling."""
